#include <time.h>

#include <algorithm>
#include <cstdlib>
#include <iostream>
#include <map>
#include <sstream>
#include <string>
#include <unordered_map>
#include <vector>

#include "Duration.h"

using namespace std;
using namespace std::chrono;

class Solution
{
public:
    int solution() {
        return 0;
    }
};

int main(int argc, char** argv)
{
    // Avoid warning
    argc = 0;
    argv = nullptr;
    // Running time
    Duration duration;

    // Variables
    int result;

    // Input

    // Main
    Solution solution;
    result = solution.solution();

    // Output
    cout << result << endl;
    // Memory usage
    return 0;
}
